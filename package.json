{
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-less": "^3.0.1",
    "gulp-notify": "^2.2.0",
    "http-server": "^0.7.5"
  },
  "scripts": {    
    "start": "npm install && bower install && gulp less && http-server public"
  },
  "gh-pages-deploy": {
    "staticpath": "public",
    "cname": "launch.etalonbag.com",
    "prep": [
      "prepare",
      "less"
    ],
    "noprompt": false
  }
}
